Codigo que crea la tabla:

CREATE TABLE productos (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    Nombre VARCHAR(100),
    Codigo VARCHAR(50),
    Precio DECIMAL(10,2),
    Cantidad INT,
    Proveedor VARCHAR(100),
    Fecha_Registro DATETIME DEFAULT CURRENT_TIMESTAMP
);